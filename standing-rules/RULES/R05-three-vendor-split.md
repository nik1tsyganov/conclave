# R05 — Split real independent implementation

Status: executable contract target; native runtime behavior still requires target-machine validation.

## MUST

The sealed plan counts independent implementation units, never planning/research/review rows. A convened plan uses min(3, implementation unit count) distinct working vendors. There is no requirement to invent three implementation units for a one-unit task. No-op implementation is rejected as participation.

## Runtime source

The matching CONCLAVE release owns the implementation and regression tests. Do not waive a failed check with a prose assertion.
