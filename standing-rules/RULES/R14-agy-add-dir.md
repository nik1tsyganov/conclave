# R14 — Mount only the staged agy context

Status: executable contract target; native runtime behavior still requires target-machine validation.

## MUST

The agy adapter mounts the assigned worktree and per-dispatch brief, rules and lean skill directories. It must not mount the entire global .claude/skills tree as the CONCLAVE seat boundary. Use pointer delivery and closed/ignored stdin as the adapter specifies. Never hard-code a source-machine username.

Every agy probe and dispatch must pin `--log-file` to `native-cli.log` in its own unique evidence directory. Do not use default second-resolution home-log names for parallel calls. The per-run native log must remain bound to the matching conversation and proof capture.

## Runtime source

The matching CONCLAVE release owns the implementation and regression tests. Do not waive a failed check with a prose assertion.
