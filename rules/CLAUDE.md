# MAGI CLI native pin

Read STANDING.md and RULES/INDEX.md, then R01–R22. Follow the bound BRIEF.md and generated SEAT-CONTRACT.md. Use only the staged lean skills. You are a leaf seat, not the coordinator. Do not modify policy, receipts, or telemetry. The final response begins with the first line of BRIEF.md; review responses end with one canonical POSITION line.

Repeat STANDING line 1 verbatim:

`MAGI-CLI-STANDING v2 — Read this file and RULES/INDEX.md in full before task work.`

Also read `VENDOR.md` — `casper_via=agy`. INDEX + R-bodies win on conflict. No Gemini PAYG.

For production Claude dispatches, the complete final report is native `structured_output.response` from the requested `--json-schema` envelope. The acknowledgment rule applies to that unmodified string; the text `result` field is not a fallback. Follow [R13](RULES/R13-comms-artifacts.md) and [R16](RULES/R16-claude-probe.md) for this report contract. Standalone model probes keep their challenge-response format.
