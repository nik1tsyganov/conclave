---
description: MAGI activation rules
alwaysApply: false
globs: magi/**,magi-probe/**,magi-kit/**,.cursor/skills/magi/**,commands/magi.md
---
If this chat is CONCLAVE, ignore this file.

Otherwise:
- Three-vendor implement split before first write.
- 60% floor.
- Idle Casper = FAILED.
- WRITE AUDIT required.
- `activation-check` required (calls `hog-check`).
- Canonical command for any repo: `node C:\src\magi\tools\activation-check.js magi-dispatch-log.jsonl`.
