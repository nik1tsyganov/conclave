---
description: MAGI Cursor arbiter rules. Always on in every Cursor workspace; ignore in CONCLAVE chats.
alwaysApply: true
---
If this chat is CONCLAVE (`/conclave`, `camerlengo-8`), ignore this file.

Otherwise, this is the MAGI Cursor arbiter for `/magi` and `/magi-cli`. Grok does not implement. `magi-whoami` is required (mode `cursor` or `cursor-cli`). For hostMode `cursor`, use the native Task model table in `.cursor/skills/magi/references/cursor-host.md`. For operational status, see `live-check.mdc` (check live, never write "until X" as current status).

- Project slices/graphs define units, dependencies, and write scope, not vendors: follow mix-mode `distributionFloor.projectSlicePolicy` at dispatch (class table + live floor), ignore project `SLICES.md` implement/verify columns, and accept only `vendorOverride` on that run's `graph.json`.
