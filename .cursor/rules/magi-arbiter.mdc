---
description: MAGI Cursor arbiter rules
alwaysApply: false
globs: magi/**,magi-probe/**,magi-kit/**,.cursor/skills/magi/**,commands/magi.md
---
If this chat is CONCLAVE (`/conclave`, `camerlengo-8`), ignore this file.

Otherwise, this is the MAGI Cursor arbiter. Grok does not implement. `magi-whoami` is required.
