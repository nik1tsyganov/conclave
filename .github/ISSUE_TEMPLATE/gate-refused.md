---
name: A gate refused something it should have allowed
about: A plan, a seat or a tally was rejected and you believe the rule is wrong
labels: gate
---

**What you ran**

```
```

**What it said** — paste the refusal verbatim, including the code (`PLAN_SEAL_FAIL`,
`RULES_STAGE_FAIL`, `PROOF_FAIL`, `LAUNCH_RETRYABLE`, …).

```
```

**Which rule you think is wrong, and why**

**Evidence directory** — if a seat ran, `out/<dispatchId>/` holds the capture, the native log,
the scope audit and the receipt. Say what is in them rather than pasting everything.
